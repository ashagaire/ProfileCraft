import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#f19f41",
    },
    background: {},
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;
